Etapa 1: sin uso de ia

Etapa 2 — Componente RecetaCard
1. Crea src/components/RecetaCard.jsx. 
2. El componente recibe por props: nombre, origen, porciones, categoria, descripcion, ingredientes. 
3. Muestra la información de forma visual y ordenada. 
4. Aplica un estilo diferenciado según la categoría (un color/etiqueta distinto para Entrada, Fondo, Postre).

Utilidad del prompt:
el prompt realizado ayudo a completar la fase 2 sin ningun problema por lo que no se realizaron correcciones mayores

Etapa 3 — Lista de recetas 
1.Crea src/components/ListaRecetas.jsx que reciba el array de recetas por props.
2. Recorre el array con .map() y renderiza un RecetaCard por cada receta. Programación Front End (TI3031) — Sumativa U3 · V4 Página 5 
3. Asigna correctamente la prop key usando el id de cada elemento (no uses el índice del array).
4. En App.jsx, renderiza ListaRecetas pasándole los datos importados
utilidad del prompt:

Etapa 4 — Estado y filtro seguro:
1. Crea src/components/FiltroCategoria.jsx con botones o un selector para filtrar por la categoría: “Todas”, “Entrada”, “Fondo”, “Postre”. 
2. En App.jsx, usa useState para guardar el filtro seleccionado y un campo de búsqueda por nombre. 
3. Aplica ambos filtros sobre el array antes de pasarlo a ListaRecetas. 
4. Usa renderizado condicional para mostrar el mensaje “No hay recetas que coincidan” cuando el resultado quede vacío. 



Etapa 5 — Destacar las recetas vegetarianas
1. Resalta visualmente los elementos con esVegetariana: true (por ejemplo, una etiqueta “VEG” o un borde distinto). 
2. Agrega un contador que muestre cuántas recetas son vegetarianas.
3. Verifica que toda la app corre sin errores ni warnings en consola.  